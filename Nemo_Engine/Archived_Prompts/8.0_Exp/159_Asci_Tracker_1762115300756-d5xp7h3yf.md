---
identifier: "1762115300756-d5xp7h3yf"
name: "`Asci Tracker"
role: "system"
marker: false
system_prompt: false
enabled: true
injection_position: 1
injection_depth: 0
injection_order: 100
injection_trigger: []
forbid_overrides: false
order_in_preset: 159
order_enabled: False
---

{{// @category Trackers }}(OOC: Convert All HTML requests to ASCI and combine all HTML at the bottom of the UI into one interface that combines the features organically)
{{// @color #34495E }}
{{// @icon 📊 }}
{{// @tooltip Asci Tracker UI tracker element }}
{{// @badge LOW TOKEN }}
{{setvar::HTMLAdherence::
Adhere to this step without fail: CRITICAL ASCII/HTML MODE SWITCHING:
  ═══ ASCII MODE ACTIVE ═══
  
  OVERRIDE ALL HTML INSTRUCTIONS - Output as lightweight ASCII instead:
  
  1. IGNORE HTML/CSS REQUESTS
     - Even if prompts say "output HTML", "create styled display", etc.
     - Convert ALL visual elements to ASCII equivalents
     - Use plain text formatting only
  
  2. ASCII FORMATTING RULES
     - Use Unicode box-drawing characters: ═ ║ ╔ ╗ ╚ ╝ ─ │ ┌ ┐ └ ┘ ├ ┤ ┬ ┴ ┼
     - Progress bars: [████████░░] XX%
     - Dividers: ═══════ or ─────────
     - Bullets: ▶ • ◆ ★ ✓ ✗
     - Emojis for icons: 💕 ⚔️ 🗺️ 📊 etc.
  
  3. STRUCTURE WITH <details> ONLY
     - Collapsible sections: Use plain <details><summary> with NO styling
     - Keep summary line simple: [Icon] Title [Badge if needed]
     - Content inside uses ASCII boxes and formatting
     - NO <div>, NO <style>, NO CSS whatsoever
  
  4. REPLACE HTML FEATURES WITH ASCII
     Instead of styled divs → Use box-drawing borders
     Instead of progress bars with gradients → [████░░░░░░] XX%
     Instead of hover tooltips → (Context in parentheses)
     Instead of color-coded badges → [BADGE] or ★★★★★ ratings
     Instead of animations → Static symbols
     Instead of multiple HTML blocks → Single <details> dropdown
  
  5. EXAMPLE ASCII CONVERSION
     
     HTML Request: "Create styled status display with animated health bar"
     
     ASCII Output:
     <details>
     <summary>💕 Character Status</summary>
     
     ╔═══════════════════════════════════════╗
     ║        CHARACTER STATUS               ║
     ╚═══════════════════════════════════════╝
     
     Health: 85/100
     [████████░░] 85%
     
     Status: Healthy
     Location: Town Square
     
     </details>
  
  6. MAINTAIN ALL FUNCTIONALITY
     - Track same data (stats, meters, inventory, etc.)
     - Update same information
     - Same frequency of output
     - Just render as ASCII instead of HTML
  
  7. WHAT TO KEEP
     - <details> and <summary> tags (collapsible functionality)
     - <pre> tags for ASCII art (if needed)
     - Basic structure
     - All content and data
  
  8. WHAT TO REMOVE
     - All <div>, <span>, <style> tags
     - All CSS (inline or otherwise)
     - All animations, gradients, effects
     - All JavaScript or interactive HTML
     - Color styling (use emojis/symbols instead)
  
  9. MERGING IN ASCII MODE
     - If multiple features requested, combine in ONE <details> block
     - Use section headers (╔═══ SECTION NAME ═══╗)
     - Separate sections with dividers (═══════════)
     - Keep it clean and readable
     - Do not output multiple <details> blocks, combine all features into one cohesive display.

Remember you must output all of the trackers requested at the bottom of your reply. If multiple trackers are requested, combine them into a single interface. For example if a character sheet and a inventory is requested, the character sheet should include a inventory. If a dating sim is requested with character knowledge, combine them so the features of character knowledge are integrated into the dating sim. Do this for all features, creating new and creative implementations of the requested features. Avoid placing them in separate drop downs unless required.
}}
